#include "bits/stdc++.h"
using namespace std;

int main(){
    int a = 4;
    int b = 6;
    // cout <<"a & b = " << (a & b) << endl;
    // cout <<"a | b = " << (a | b) <<  endl;
    // cout <<"~a  = " << (~ a) << endl;
    // cout <<"a ^ b = " << (a ^ b) <<  endl;

    // Left shift - denoted by "<<" 
    // for eg 5 = 101:
    // if we "5 << 1" - 1010: read as 5 is shifted to left by 1 bit
    // padding is always with 0 in case of +ve numbers and depends on compiler
    // in majority cases it is multiplication by 2

    // int x = 5;
    // cout << (5 << 1) <<endl; 


    // right shift - denoted by ">>" 
    // for eg 5 = 101:
    // if we "5 >> 1" - 010: read as 5 is shifted to right by 1 bit
    // in majority cases it is divided by 2
    // padding is always with 0 in case of +ve numbers and depends on compiler
    // int x = 5;
    // cout << (5 >> 1) <<endl; 



    // ----------to convert decimal to binary----------
    // int n;
    // cin >> n;

    // int ans = 0;
    // int i = 0;

    // while (n != 0)
    // {
    //     int bit = n & 1;
    //     ans = ans * 10 + bit;

    //     n = n >> 1;
    // }
    // cout  << ans;  

    //----------To convert from decimal to binary----------
    // int n = 10;
    // int ans = 0;
    // int i = 0;

    // while (n != 0)
    // {
    //     int digit = n % 10;
    //     if(digit == 1){
    //         ans = ans + pow(2,i);
    //     }
    //     n = n/10;
    //     i++;
    // }

    // cout << ans;
    
    // ----------to find compliment of a number----------
    




    return 0;
}